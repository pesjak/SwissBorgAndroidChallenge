package com.primoz.swissborgandroidchallenge.helpers

import com.primoz.swissborgandroidchallenge.R

enum class FilterType(val stringResource: Int) {
    SORT_GAIN(R.string.sort_gain),
    SORT_LOSS(R.string.sort_loss)
}
